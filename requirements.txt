# Framework web Python
Flask==3.0.0

# Xử lý database SQLite (built-in Python, không cần cài)
# sqlite3

# Thư viện bắt buộc: xử lý date/time
python-dateutil==2.8.2

# Thư viện bắt buộc: tạo mã đơn hàng ngẫu nhiên
shortuuid==1.0.11